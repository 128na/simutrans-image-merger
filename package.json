{
  "description": "install scripts for simutrans-image-marger",
  "scripts": {
    "pip:freeze": "pip freeze > requirements.txt",
    "py:install": "pip install -r requirements.txt",
    "py:build": "pyinstaller -F ./src/simutrans-image-merger.py"
  }
}